image: docker:latest

stages:
  - test

services:
  - docker:dind

tests:
  stage: test
  script:
    - apk add --no-cache py-pip
    - pip install --upgrade pip
    - pip install docker-compose
    - docker-compose run --rm tests
