image: docker:latest

stages:
  - test
  - build
  - push

services:
  - docker:dind

before_script:
  - docker login --username=$DOCKER_USER --password=$DOCKER_PASS

tests:
  stage: test
  script:
    - apk add --no-cache py-pip python-dev libffi-dev openssl-dev gcc libc-dev make
    - pip install --upgrade pip
    - pip install docker-compose
    - docker-compose run --rm tests

build:
  stage: build
  script:
    - docker build --pull -t ovski/account-statement-parser:latest .

push:
  stage: push
  script:
    - docker build --pull -t ovski/account-statement-parser:latest .
    - docker push ovski/account-statement-parser:latest
  only:
    - master
